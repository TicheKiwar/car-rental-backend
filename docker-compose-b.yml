services:
  backend:
      build: 
        context: ./
        dockerfile: Dockerfile
      ports:
        - "3000:3000"
      environment:
        - NODE_ENV=production
      networks:
        - app_network
networks:
  app_network: